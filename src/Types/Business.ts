export type BusinessDataType = {
    name: string;
    birthday: string;
    instagram: string;
    followers: number;
    verified: boolean;
    email: string;
};

export type BusinesssType = {
    id: string
    data: BusinessDataType
}